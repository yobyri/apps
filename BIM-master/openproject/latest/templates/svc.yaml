apiVersion: v1
kind: Service
metadata:
  name: openproject
  labels:
    app: openproject
spec:
  type: NodePort
  ports:
  - name: openproject
    targetPort: openproject
    port: 30000
  selector:
    app: openproject
